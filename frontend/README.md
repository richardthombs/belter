# Frontend (PixiJS + Vite)

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start the dev server:
   ```sh
   npm run dev
   ```

## Features
- PixiJS rendering
- WebSocket connection to backend
