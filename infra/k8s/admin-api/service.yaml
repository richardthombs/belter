apiVersion: v1
kind: Service
metadata:
  name: admin-api
spec:
  selector:
    app: admin-api
  ports:
    - port: 5002
      targetPort: 5002
  # Internal only — never publicly exposed
  type: ClusterIP
