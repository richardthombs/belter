apiVersion: v1
kind: Service
metadata:
  name: shard
spec:
  selector:
    app: shard
  ports:
    - port: 5001
      targetPort: 5001
  # Internal only — not exposed outside the cluster
  type: ClusterIP
